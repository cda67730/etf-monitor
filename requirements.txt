# Cloud Run 版本的 requirements.txt

# FastAPI 核心
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 模板引擎
jinja2==3.1.2

# 表單處理
python-multipart==0.0.6

# HTTP 請求
requests==2.31.0

# 檔案處理
aiofiles==23.2.1

# 環境變數處理
python-dotenv==1.0.0

# 生產環境伺服器 (Cloud Run 推薦)
gunicorn==21.2.0

# 結構化日誌 (雲端友善)
structlog==23.2.0

# Google Cloud 日誌 (可選，用於進階監控)
google-cloud-logging==3.8.0

# 如果未來要使用 PostgreSQL (目前用 SQLite 可以先註解)
# psycopg2-binary==2.9.7

# 如果要使用 Redis 快取 (可選)
# redis==5.0.1
# fastapi-cache2==0.2.1

# 安全相關
cryptography==41.0.7

# 時間處理 (如果需要更複雜的時區處理)
# pytz==2023.3

# 資料驗證 (FastAPI 內建，但明確列出版本)
pydantic==2.5.0

# JSON 處理優化 (可選)
# orjson==3.9.10